diff --git a/dist/client/index.js b/dist/client/index.js
index de76da23ad9f3ab4dbeac19b58144b9ceef08349..cd6991cc49203115f3a629357f8d800a7dfd1dde 100644
--- a/dist/client/index.js
+++ b/dist/client/index.js
@@ -48,9 +48,6 @@ export class Polar {
     }
     /** Create a Polar checkout session, optionally with a free trial period. */
     async createCheckoutSession(ctx, { productIds, userId, email, origin, successUrl, subscriptionId, metadata, trialInterval, trialIntervalCount, }) {
-        const dbCustomer = await ctx.runQuery(this.component.lib.getCustomerByUserId, {
-            userId,
-        });
         const getOrCreateCustomer = async () => {
             // Check if a customer with this email already exists in Polar
             const existing = await customersList(this.polar, { email, limit: 1 });
@@ -72,13 +69,14 @@ export class Polar {
             }
             return customer.value;
         };
-        const customerId = dbCustomer?.id || (await getOrCreateCustomer()).id;
-        if (!dbCustomer) {
-            await ctx.runMutation(this.component.lib.insertCustomer, {
-                id: customerId,
-                userId,
-            });
-        }
+        // Always resolve against Polar by email — never trust a stale local id alone
+        // (e.g. after the Polar customer was deleted in the dashboard).
+        const polarCustomer = await getOrCreateCustomer();
+        const customerId = polarCustomer.id;
+        await ctx.runMutation(this.component.lib.insertCustomer, {
+            id: customerId,
+            userId,
+        });
         const checkout = await checkoutsCreate(this.polar, {
             allowDiscountCodes: true,
             customerId,
diff --git a/dist/component/lib.js b/dist/component/lib.js
index 76c3b2ae89ae94f6bc5a8208727892b33625510f..ab2b1842fb5cafea18afde8c3a22f316d39c34e5 100644
--- a/dist/component/lib.js
+++ b/dist/component/lib.js
@@ -28,6 +28,15 @@ export const insertCustomer = mutation({
             .withIndex("userId", (q) => q.eq("userId", args.userId))
             .unique();
         if (existingCustomer) {
+            // Upsert Polar customer id — local rows can go stale if the customer
+            // was deleted/recreated in Polar.
+            if (existingCustomer.id !== args.id ||
+                JSON.stringify(existingCustomer.metadata) !== JSON.stringify(args.metadata)) {
+                await ctx.db.patch(existingCustomer._id, {
+                    id: args.id,
+                    metadata: args.metadata,
+                });
+            }
             return existingCustomer._id;
         }
         return ctx.db.insert("customers", {
diff --git a/src/client/index.ts b/src/client/index.ts
index a5c012824f08b0a13912d544f0695aaee2e84eac..d8259829d3e06f30e4ff9b9259013a99b382a84d 100644
--- a/src/client/index.ts
+++ b/src/client/index.ts
@@ -133,12 +133,6 @@ export class Polar<
       trialIntervalCount?: number | null;
     },
   ): Promise<Checkout> {
-    const dbCustomer = await ctx.runQuery(
-      this.component.lib.getCustomerByUserId,
-      {
-        userId,
-      },
-    );
     const getOrCreateCustomer = async () => {
       // Check if a customer with this email already exists in Polar
       const existing = await customersList(this.polar, { email, limit: 1 });
@@ -160,13 +154,14 @@ export class Polar<
       }
       return customer.value;
     };
-    const customerId = dbCustomer?.id || (await getOrCreateCustomer()).id;
-    if (!dbCustomer) {
-      await ctx.runMutation(this.component.lib.insertCustomer, {
-        id: customerId,
-        userId,
-      });
-    }
+    // Always resolve against Polar by email — never trust a stale local id alone
+    // (e.g. after the Polar customer was deleted in the dashboard).
+    const polarCustomer = await getOrCreateCustomer();
+    const customerId = polarCustomer.id;
+    await ctx.runMutation(this.component.lib.insertCustomer, {
+      id: customerId,
+      userId,
+    });
     const checkout = await checkoutsCreate(this.polar, {
       allowDiscountCodes: true,
       customerId,
diff --git a/src/component/lib.ts b/src/component/lib.ts
index 136d2fcb54270d2b525fa43ee8fcda8f795ebd1a..7ebb40fc4cfe45759e6d74732eee0979260b28c7 100644
--- a/src/component/lib.ts
+++ b/src/component/lib.ts
@@ -31,6 +31,17 @@ export const insertCustomer = mutation({
       .withIndex("userId", (q) => q.eq("userId", args.userId))
       .unique();
     if (existingCustomer) {
+      // Upsert Polar customer id — local rows can go stale if the customer
+      // was deleted/recreated in Polar.
+      if (
+        existingCustomer.id !== args.id ||
+        JSON.stringify(existingCustomer.metadata) !== JSON.stringify(args.metadata)
+      ) {
+        await ctx.db.patch(existingCustomer._id, {
+          id: args.id,
+          metadata: args.metadata,
+        });
+      }
       return existingCustomer._id;
     }
     return ctx.db.insert("customers", {
